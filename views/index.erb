
<%= erb :'partials/_topnav' %>

<div class="tab-content">
  <section id="listing_container" class="container tab-pane active">
    <div class="row">
      <aside class="actions">
        <ul class="subNav nav nav-tabs nav-stacked">
          <li class="active"><a id="subNavListings" href="#listings"><i class="icon-list"></i>Listings</a></li>
          <li class=""><a href="#create"><i class="icon-pencil"></i>New listing</a></li>
          <li><a href="#direct"><i class="icon-gift"></i>Direct Payments</a></li>
        </ul>
      </aside>

      <div id="listing_content" class="inner-content">
        <div class="tab-content">
          <%= erb :'modals/_individualListing' %>
          <div id="listings" class="test tab-pane active">

            <section class="threefourth receipts receipt_view">
              <ul id="receiptList">
                <%= erb :'partials/_receipts' %>
              </ul>
            </section>

            <div class="details-container full">
              <%= erb :'partials/_listingFilters' %>
            </div>
          </div> <!--listings-->

          <section id="create" class="tab-pane" >
            <%= erb :'partials/_createListing' %>
          </section>

          <section id="direct" class="tab-pane">
            <%= erb :'partials/_directPayment' %>
          </section>
        
        </div>   
      </div>
    
    </div> <!--row-->
  </section>

  <!-- Future content tab -->
  <%= erb :'partials/_futureContent' %>
  
</div> <!--tab-content-->

<%= erb :'modals/_globalSocialStream' %>
